#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	int r;

		i = 48;
		while (i <= 57)
		{
			putchar(i);
			i++;

		}

		r = 97;
		while (r <= 102)
		{
			putchar(r);
			r++;

		}
		putchar('\n');
		return (0);
}
