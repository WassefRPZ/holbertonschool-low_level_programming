#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	int r;

		i = 48;
		while (i <= 57)
		{
			putchar(i);
			i++;

		}

		r = 65;
		while (r <= 70)
		{
			putchar(r);
			r++;

		}
		putchar('\n');
		return (0);
}
